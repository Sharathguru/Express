// import { createServer } from "http";
// import app from "./app.js"; // Corrected relative path

// let PORT = 3000;
// let server = createServer(app);

// server.listen(PORT, () => console.log(`Server running on port ${PORT}`));


import express from "express"
let app=express()

app.listen(3000,()=>{
    
})